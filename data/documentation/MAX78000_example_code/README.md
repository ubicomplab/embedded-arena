# MAX78000 Example Code

This public package keeps the canonical benchmark seed firmware under `firmware/`. Documentation variants copy this lightweight directory as a signpost rather than duplicating firmware trees in the sandbox.
